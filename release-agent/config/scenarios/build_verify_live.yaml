# Test Phase 2 END-TO-END against the REAL pipelines: fast-forwards Phases 0-1
# offline, then runs the four verification steps LIVE (real `az` reads of the
# 2026-08 checker → orchestrator → ECS/Local MRWP runs) and halts at the go_test
# gate. This is the "validate Phase 2 for real" scenario.
#
#   release-agent sim run --scenario build_verify_live
#
name: build_verify_live
release_id: 2026-08
ccd: 2026-08-26
ccd_source: confirmed
as_of: CCD+2
target: { phase: build_verify, at: gate }
data: live          # the 4 build_verify steps hit real az; earlier phases are mocked
