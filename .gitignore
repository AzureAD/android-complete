# Ignore Gradle project-specific cache directory
.gradle

# Android Studio memory dump file.
.hprof

# Ignore existing repos held under the same structure
msal
adal
broker
common
authenticator
azuresample
oneauth
tsl
msalcpp
design-docs

# Gradle files
.gradle/
build/

# IntelliJ
*.iml
.idea
.vscode/*
!.vscode/mcp.json
extensions/feature-orchestrator/.vscode/
extensions/feature-orchestrator/*.vsix
.settings
.project

# Local configuration file (sdk path, etc)
gradle.properties
local.properties

# Developer-local config (GitHub accounts, personal settings — never commit)
.github/developer-local.json

# Design review comments (personal, per-developer)
.github/design-reviews/

#nodejs
node_modules

# OSX file
.DS_Store

# Windows thumbnail cache files
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db

# Folder config file
[Dd]esktop.ini

out/

plugins/buildsystem/bin
# Feature Orchestrator developer-local config (per-developer, not committed)
.github/developer-local.json
/.github/orchestrator-config.json
/.github/codebase-context.md
